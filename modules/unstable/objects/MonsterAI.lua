-- Action resolution for monsters occurs for all monsters 'at once'.
-- That is to say, each monster determines its target via some targeting strategy.

local M = nilprotect {} -- Submodule

-- Sets the 'ai_state' table fields for a monster. 
function M.resolve_monster_actions()

end

return M