/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2Color
 *
 * This file has been generated by dub 2.1.~.
 */
#include "dub/dub.h"
#include "Box2D/Common/b2Draw.h"


/** ~b2Color()
 * 
 */
static int b2Color__b2Color(lua_State *L) {
  try {
    DubUserdata *userdata = ((DubUserdata*)dub_checksdata_d(L, 1, "b2.Color"));
    if (userdata->gc) {
      b2Color *self = (b2Color *)userdata->ptr;
      delete self;
    }
    userdata->gc = false;
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "~b2Color: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "~b2Color: Unknown exception");
  }
  return dub_error(L);
}

/** Set attributes (key, value)
 * 
 */
static int b2Color__set_(lua_State *L) {

  b2Color *self = *((b2Color **)dub_checksdata_n(L, 1, "b2.Color"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub_hash(key, 3);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "r")) break;
      self->r = luaL_checknumber(L, 3);
      return 0;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "g")) break;
      self->g = luaL_checknumber(L, 3);
      return 0;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "b")) break;
      self->b = luaL_checknumber(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2Color__get_(lua_State *L) {

  b2Color *self = *((b2Color **)dub_checksdata_n(L, 1, "b2.Color", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub_hash(key, 3);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "r")) break;
      lua_pushnumber(L, self->r);
      return 1;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "g")) break;
      lua_pushnumber(L, self->g);
      return 1;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "b")) break;
      lua_pushnumber(L, self->b);
      return 1;
    }
  }
  return 0;
}

/** b2Color::b2Color()
 * vendor/Box2D/Box2D/Common/b2Draw.h:27
 */
static int b2Color_b2Color(lua_State *L) {
  try {
    int top__ = lua_gettop(L);
    if (top__ >= 3) {
      float32 r = dub_checknumber(L, 1);
      float32 g = dub_checknumber(L, 2);
      float32 b = dub_checknumber(L, 3);
      b2Color *retval__ = new b2Color(r, g, b);
      dub_pushudata(L, retval__, "b2.Color", true);
      return 1;
    } else {
      b2Color *retval__ = new b2Color();
      dub_pushudata(L, retval__, "b2.Color", true);
      return 1;
    }
  } catch (std::exception &e) {
    lua_pushfstring(L, "b2Color: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "b2Color: Unknown exception");
  }
  return dub_error(L);
}

/** void b2Color::Set(float32 ri, float32 gi, float32 bi)
 * vendor/Box2D/Box2D/Common/b2Draw.h:29
 */
static int b2Color_Set(lua_State *L) {
  try {
    b2Color *self = *((b2Color **)dub_checksdata(L, 1, "b2.Color"));
    float32 ri = dub_checknumber(L, 2);
    float32 gi = dub_checknumber(L, 3);
    float32 bi = dub_checknumber(L, 4);
    self->Set(ri, gi, bi);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "Set: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "Set: Unknown exception");
  }
  return dub_error(L);
}



// --=============================================== __tostring
static int b2Color___tostring(lua_State *L) {
  b2Color *self = *((b2Color **)dub_checksdata_n(L, 1, "b2.Color"));
  lua_pushfstring(L, "b2.Color: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2Color_member_methods[] = {
  { "__gc"         , b2Color__b2Color     },
  { "__newindex"   , b2Color__set_        },
  { "__index"      , b2Color__get_        },
  { "new"          , b2Color_b2Color      },
  { "Set"          , b2Color_Set          },
  { "__tostring"   , b2Color___tostring   },
  { "deleted"      , dub_isDeleted        },
  { NULL, NULL},
};


extern "C" int luaopen_b2_Color(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.Color");
  // <mt>

  // register member methods
  luaL_register(L, NULL, b2Color_member_methods);
  // save meta-table in b2
  dub_register(L, "b2", "Color", "Color");
  // <mt>
  lua_pop(L, 1);
  return 0;
}
