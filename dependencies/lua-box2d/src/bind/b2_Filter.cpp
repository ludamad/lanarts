/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2Filter
 *
 * This file has been generated by dub 2.1.~.
 */
#include "dub/dub.h"
#include "Box2D/Dynamics/b2Fixture.h"


/** ~b2Filter()
 * 
 */
static int b2Filter__b2Filter(lua_State *L) {
  try {
    DubUserdata *userdata = ((DubUserdata*)dub_checksdata_d(L, 1, "b2.Filter"));
    if (userdata->gc) {
      b2Filter *self = (b2Filter *)userdata->ptr;
      delete self;
    }
    userdata->gc = false;
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "~b2Filter: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "~b2Filter: Unknown exception");
  }
  return dub_error(L);
}

/** Set attributes (key, value)
 * 
 */
static int b2Filter__set_(lua_State *L) {

  b2Filter *self = *((b2Filter **)dub_checksdata_n(L, 1, "b2.Filter"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub_hash(key, 4);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "categoryBits")) break;
      self->categoryBits = luaL_checkint(L, 3);
      return 0;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "maskBits")) break;
      self->maskBits = luaL_checkint(L, 3);
      return 0;
    }
    case 3: {
      if (DUB_ASSERT_KEY(key, "groupIndex")) break;
      self->groupIndex = luaL_checkint(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2Filter__get_(lua_State *L) {

  b2Filter *self = *((b2Filter **)dub_checksdata_n(L, 1, "b2.Filter", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub_hash(key, 4);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "categoryBits")) break;
      lua_pushnumber(L, self->categoryBits);
      return 1;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "maskBits")) break;
      lua_pushnumber(L, self->maskBits);
      return 1;
    }
    case 3: {
      if (DUB_ASSERT_KEY(key, "groupIndex")) break;
      lua_pushnumber(L, self->groupIndex);
      return 1;
    }
  }
  return 0;
}

/** b2Filter::b2Filter()
 * vendor/Box2D/Box2D/Dynamics/b2Fixture.h:35
 */
static int b2Filter_b2Filter(lua_State *L) {
  try {
    b2Filter *retval__ = new b2Filter();
    dub_pushudata(L, retval__, "b2.Filter", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "b2Filter: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "b2Filter: Unknown exception");
  }
  return dub_error(L);
}



// --=============================================== __tostring
static int b2Filter___tostring(lua_State *L) {
  b2Filter *self = *((b2Filter **)dub_checksdata_n(L, 1, "b2.Filter"));
  lua_pushfstring(L, "b2.Filter: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2Filter_member_methods[] = {
  { "__gc"         , b2Filter__b2Filter   },
  { "__newindex"   , b2Filter__set_       },
  { "__index"      , b2Filter__get_       },
  { "new"          , b2Filter_b2Filter    },
  { "__tostring"   , b2Filter___tostring  },
  { "deleted"      , dub_isDeleted        },
  { NULL, NULL},
};


extern "C" int luaopen_b2_Filter(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.Filter");
  // <mt>

  // register member methods
  luaL_register(L, NULL, b2Filter_member_methods);
  // save meta-table in b2
  dub_register(L, "b2", "Filter", "Filter");
  // <mt>
  lua_pop(L, 1);
  return 0;
}
