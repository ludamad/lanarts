/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2MassData
 *
 * This file has been generated by dub 2.1.~.
 */
#include "dub/dub.h"
#include "Box2D/Collision/Shapes/b2Shape.h"


/** Set attributes (key, value)
 * 
 */
static int b2MassData__set_(lua_State *L) {

  b2MassData *self = *((b2MassData **)dub_checksdata_n(L, 1, "b2.MassData"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub_hash(key, 5);
  switch(key_h) {
    case 2: {
      if (DUB_ASSERT_KEY(key, "mass")) break;
      self->mass = luaL_checknumber(L, 3);
      return 0;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "center")) break;
      self->center = **((b2Vec2 **)dub_checksdata_n(L, 3, "b2.Vec2"));
      return 0;
    }
    case 3: {
      if (DUB_ASSERT_KEY(key, "I")) break;
      self->I = luaL_checknumber(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2MassData__get_(lua_State *L) {

  b2MassData *self = *((b2MassData **)dub_checksdata_n(L, 1, "b2.MassData", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub_hash(key, 5);
  switch(key_h) {
    case 2: {
      if (DUB_ASSERT_KEY(key, "mass")) break;
      lua_pushnumber(L, self->mass);
      return 1;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "center")) break;
      dub_pushudata(L, &self->center, "b2.Vec2", false);
      return 1;
    }
    case 3: {
      if (DUB_ASSERT_KEY(key, "I")) break;
      lua_pushnumber(L, self->I);
      return 1;
    }
  }
  return 0;
}

/** b2MassData()
 * 
 */
static int b2MassData_b2MassData(lua_State *L) {
  try {
    b2MassData *retval__ = new b2MassData();
    dub_pushudata(L, retval__, "b2.MassData", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "b2MassData: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "b2MassData: Unknown exception");
  }
  return dub_error(L);
}



// --=============================================== __tostring
static int b2MassData___tostring(lua_State *L) {
  b2MassData *self = *((b2MassData **)dub_checksdata_n(L, 1, "b2.MassData"));
  lua_pushfstring(L, "b2.MassData: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2MassData_member_methods[] = {
  { "__newindex"   , b2MassData__set_     },
  { "__index"      , b2MassData__get_     },
  { "new"          , b2MassData_b2MassData },
  { "__tostring"   , b2MassData___tostring },
  { "deleted"      , dub_isDeleted        },
  { NULL, NULL},
};


extern "C" int luaopen_b2_MassData(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.MassData");
  // <mt>

  // register member methods
  luaL_register(L, NULL, b2MassData_member_methods);
  // save meta-table in b2
  dub_register(L, "b2", "MassData", "MassData");
  // <mt>
  lua_pop(L, 1);
  return 0;
}
