/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2RevoluteJointDef
 *
 * This file has been generated by dub 2.1.~.
 */
#include "dub/dub.h"
#include "Box2D/Dynamics/Joints/b2RevoluteJoint.h"


/** ~b2RevoluteJointDef()
 * 
 */
static int b2RevoluteJointDef__b2RevoluteJointDef(lua_State *L) {
  try {
    DubUserdata *userdata = ((DubUserdata*)dub_checksdata_d(L, 1, "b2.RevoluteJointDef"));
    if (userdata->gc) {
      b2RevoluteJointDef *self = (b2RevoluteJointDef *)userdata->ptr;
      delete self;
    }
    userdata->gc = false;
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "~b2RevoluteJointDef: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "~b2RevoluteJointDef: Unknown exception");
  }
  return dub_error(L);
}

/** Set attributes (key, value)
 * 
 */
static int b2RevoluteJointDef__set_(lua_State *L) {

  b2RevoluteJointDef *self = *((b2RevoluteJointDef **)dub_checksdata_n(L, 1, "b2.RevoluteJointDef"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub_hash(key, 38);
  switch(key_h) {
    case 37: {
      if (DUB_ASSERT_KEY(key, "localAnchorA")) break;
      self->localAnchorA = **((b2Vec2 **)dub_checksdata_n(L, 3, "b2.Vec2"));
      return 0;
    }
    case 0: {
      if (DUB_ASSERT_KEY(key, "localAnchorB")) break;
      self->localAnchorB = **((b2Vec2 **)dub_checksdata_n(L, 3, "b2.Vec2"));
      return 0;
    }
    case 16: {
      if (DUB_ASSERT_KEY(key, "referenceAngle")) break;
      self->referenceAngle = luaL_checknumber(L, 3);
      return 0;
    }
    case 18: {
      if (DUB_ASSERT_KEY(key, "enableLimit")) break;
      self->enableLimit = luaL_checkboolean(L, 3);
      return 0;
    }
    case 24: {
      if (DUB_ASSERT_KEY(key, "lowerAngle")) break;
      self->lowerAngle = luaL_checknumber(L, 3);
      return 0;
    }
    case 23: {
      if (DUB_ASSERT_KEY(key, "upperAngle")) break;
      self->upperAngle = luaL_checknumber(L, 3);
      return 0;
    }
    case 12: {
      if (DUB_ASSERT_KEY(key, "enableMotor")) break;
      self->enableMotor = luaL_checkboolean(L, 3);
      return 0;
    }
    case 32: {
      if (DUB_ASSERT_KEY(key, "motorSpeed")) break;
      self->motorSpeed = luaL_checknumber(L, 3);
      return 0;
    }
    case 29: {
      if (DUB_ASSERT_KEY(key, "maxMotorTorque")) break;
      self->maxMotorTorque = luaL_checknumber(L, 3);
      return 0;
    }
    case 28: {
      if (DUB_ASSERT_KEY(key, "type")) break;
      self->type = (b2JointType)luaL_checkint(L, 3);
      return 0;
    }
    case 25: {
      if (DUB_ASSERT_KEY(key, "userData")) break;
      dub_protect(L, 1, 3, "userData");
      self->userData = *((void **)dub_checksdata_n(L, 3, "void"));
      return 0;
    }
    case 7: {
      if (DUB_ASSERT_KEY(key, "bodyA")) break;
      dub_protect(L, 1, 3, "bodyA");
      self->bodyA = *((b2Body **)dub_checksdata_n(L, 3, "b2.Body"));
      return 0;
    }
    case 8: {
      if (DUB_ASSERT_KEY(key, "bodyB")) break;
      dub_protect(L, 1, 3, "bodyB");
      self->bodyB = *((b2Body **)dub_checksdata_n(L, 3, "b2.Body"));
      return 0;
    }
    case 15: {
      if (DUB_ASSERT_KEY(key, "collideConnected")) break;
      self->collideConnected = luaL_checkboolean(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2RevoluteJointDef__get_(lua_State *L) {

  b2RevoluteJointDef *self = *((b2RevoluteJointDef **)dub_checksdata_n(L, 1, "b2.RevoluteJointDef", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub_hash(key, 38);
  switch(key_h) {
    case 37: {
      if (DUB_ASSERT_KEY(key, "localAnchorA")) break;
      dub_pushudata(L, &self->localAnchorA, "b2.Vec2", false);
      return 1;
    }
    case 0: {
      if (DUB_ASSERT_KEY(key, "localAnchorB")) break;
      dub_pushudata(L, &self->localAnchorB, "b2.Vec2", false);
      return 1;
    }
    case 16: {
      if (DUB_ASSERT_KEY(key, "referenceAngle")) break;
      lua_pushnumber(L, self->referenceAngle);
      return 1;
    }
    case 18: {
      if (DUB_ASSERT_KEY(key, "enableLimit")) break;
      lua_pushboolean(L, self->enableLimit);
      return 1;
    }
    case 24: {
      if (DUB_ASSERT_KEY(key, "lowerAngle")) break;
      lua_pushnumber(L, self->lowerAngle);
      return 1;
    }
    case 23: {
      if (DUB_ASSERT_KEY(key, "upperAngle")) break;
      lua_pushnumber(L, self->upperAngle);
      return 1;
    }
    case 12: {
      if (DUB_ASSERT_KEY(key, "enableMotor")) break;
      lua_pushboolean(L, self->enableMotor);
      return 1;
    }
    case 32: {
      if (DUB_ASSERT_KEY(key, "motorSpeed")) break;
      lua_pushnumber(L, self->motorSpeed);
      return 1;
    }
    case 29: {
      if (DUB_ASSERT_KEY(key, "maxMotorTorque")) break;
      lua_pushnumber(L, self->maxMotorTorque);
      return 1;
    }
    case 28: {
      if (DUB_ASSERT_KEY(key, "type")) break;
      lua_pushnumber(L, self->type);
      return 1;
    }
    case 25: {
      if (DUB_ASSERT_KEY(key, "userData")) break;
      void *retval__ = self->userData;
      if (!retval__) return 0;
      dub_pushudata(L, retval__, "void", false);
      return 1;
    }
    case 7: {
      if (DUB_ASSERT_KEY(key, "bodyA")) break;
      b2Body *retval__ = self->bodyA;
      if (!retval__) return 0;
      dub_pushudata(L, retval__, "b2.Body", false);
      return 1;
    }
    case 8: {
      if (DUB_ASSERT_KEY(key, "bodyB")) break;
      b2Body *retval__ = self->bodyB;
      if (!retval__) return 0;
      dub_pushudata(L, retval__, "b2.Body", false);
      return 1;
    }
    case 15: {
      if (DUB_ASSERT_KEY(key, "collideConnected")) break;
      lua_pushboolean(L, self->collideConnected);
      return 1;
    }
  }
  return 0;
}

/** Cast (class_name)
 * 
 */
static int b2RevoluteJointDef__cast_(lua_State *L) {

  b2RevoluteJointDef *self = *((b2RevoluteJointDef **)dub_checksdata_n(L, 1, "b2.RevoluteJointDef"));
  const char *key = luaL_checkstring(L, 2);
  void **retval__ = (void**)lua_newuserdata(L, sizeof(void*));
  int key_h = dub_hash(key, 2);
  switch(key_h) {
    case 1: {
      if (DUB_ASSERT_KEY(key, "b2.JointDef")) break;
      *retval__ = static_cast<b2JointDef *>(self);
      return 1;
    }
  }
  return 0;
}

/** b2RevoluteJointDef::b2RevoluteJointDef()
 * vendor/Box2D/Box2D/Dynamics/Joints/b2RevoluteJoint.h:38
 */
static int b2RevoluteJointDef_b2RevoluteJointDef(lua_State *L) {
  try {
    b2RevoluteJointDef *retval__ = new b2RevoluteJointDef();
    dub_pushudata(L, retval__, "b2.RevoluteJointDef", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "b2RevoluteJointDef: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "b2RevoluteJointDef: Unknown exception");
  }
  return dub_error(L);
}

/** void b2RevoluteJointDef::Initialize(b2Body *bodyA, b2Body *bodyB, const b2Vec2 &anchor)
 * vendor/Box2D/Box2D/Dynamics/Joints/b2RevoluteJoint.h:53
 */
static int b2RevoluteJointDef_Initialize(lua_State *L) {
  try {
    b2RevoluteJointDef *self = *((b2RevoluteJointDef **)dub_checksdata(L, 1, "b2.RevoluteJointDef"));
    b2Body *bodyA = *((b2Body **)dub_checksdata(L, 2, "b2.Body"));
    b2Body *bodyB = *((b2Body **)dub_checksdata(L, 3, "b2.Body"));
    b2Vec2 *anchor = *((b2Vec2 **)dub_checksdata(L, 4, "b2.Vec2"));
    self->Initialize(bodyA, bodyB, *anchor);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "Initialize: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "Initialize: Unknown exception");
  }
  return dub_error(L);
}



// --=============================================== __tostring
static int b2RevoluteJointDef___tostring(lua_State *L) {
  b2RevoluteJointDef *self = *((b2RevoluteJointDef **)dub_checksdata_n(L, 1, "b2.RevoluteJointDef"));
  lua_pushfstring(L, "b2.RevoluteJointDef: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2RevoluteJointDef_member_methods[] = {
  { "__gc"         , b2RevoluteJointDef__b2RevoluteJointDef },
  { "__newindex"   , b2RevoluteJointDef__set_ },
  { "__index"      , b2RevoluteJointDef__get_ },
  { "_cast_"       , b2RevoluteJointDef__cast_ },
  { "new"          , b2RevoluteJointDef_b2RevoluteJointDef },
  { "Initialize"   , b2RevoluteJointDef_Initialize },
  { "__tostring"   , b2RevoluteJointDef___tostring },
  { "deleted"      , dub_isDeleted        },
  { NULL, NULL},
};


extern "C" int luaopen_b2_RevoluteJointDef(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.RevoluteJointDef");
  // <mt>

  // register member methods
  luaL_register(L, NULL, b2RevoluteJointDef_member_methods);
  // save meta-table in b2
  dub_register(L, "b2", "RevoluteJointDef", "RevoluteJointDef");
  // <mt>
  lua_pop(L, 1);
  return 0;
}
