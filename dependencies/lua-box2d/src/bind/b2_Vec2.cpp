/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2Vec2
 *
 * This file has been generated by dub 2.1.~.
 */
#include "dub/dub.h"
#include "Box2D/Common/b2Math.h"


/** ~b2Vec2()
 * 
 */
static int b2Vec2__b2Vec2(lua_State *L) {
  try {
    DubUserdata *userdata = ((DubUserdata*)dub_checksdata_d(L, 1, "b2.Vec2"));
    if (userdata->gc) {
      b2Vec2 *self = (b2Vec2 *)userdata->ptr;
      delete self;
    }
    userdata->gc = false;
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "~b2Vec2: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "~b2Vec2: Unknown exception");
  }
  return dub_error(L);
}

/** Set attributes (key, value)
 * 
 */
static int b2Vec2__set_(lua_State *L) {

  b2Vec2 *self = *((b2Vec2 **)dub_checksdata_n(L, 1, "b2.Vec2"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub_hash(key, 2);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "x")) break;
      self->x = luaL_checknumber(L, 3);
      return 0;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "y")) break;
      self->y = luaL_checknumber(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2Vec2__get_(lua_State *L) {

  b2Vec2 *self = *((b2Vec2 **)dub_checksdata_n(L, 1, "b2.Vec2", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub_hash(key, 2);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "x")) break;
      lua_pushnumber(L, self->x);
      return 1;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "y")) break;
      lua_pushnumber(L, self->y);
      return 1;
    }
  }
  return 0;
}

/** b2Vec2::b2Vec2()
 * vendor/Box2D/Box2D/Common/b2Math.h:67
 */
static int b2Vec2_b2Vec2(lua_State *L) {
  try {
    int top__ = lua_gettop(L);
    if (top__ >= 2) {
      float32 x = dub_checknumber(L, 1);
      float32 y = dub_checknumber(L, 2);
      b2Vec2 *retval__ = new b2Vec2(x, y);
      dub_pushudata(L, retval__, "b2.Vec2", true);
      return 1;
    } else {
      b2Vec2 *retval__ = new b2Vec2();
      dub_pushudata(L, retval__, "b2.Vec2", true);
      return 1;
    }
  } catch (std::exception &e) {
    lua_pushfstring(L, "b2Vec2: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "b2Vec2: Unknown exception");
  }
  return dub_error(L);
}

/** void b2Vec2::SetZero()
 * vendor/Box2D/Box2D/Common/b2Math.h:73
 */
static int b2Vec2_SetZero(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    self->SetZero();
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "SetZero: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "SetZero: Unknown exception");
  }
  return dub_error(L);
}

/** void b2Vec2::Set(float32 x_, float32 y_)
 * vendor/Box2D/Box2D/Common/b2Math.h:76
 */
static int b2Vec2_Set(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    float32 x_ = dub_checknumber(L, 2);
    float32 y_ = dub_checknumber(L, 3);
    self->Set(x_, y_);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "Set: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "Set: Unknown exception");
  }
  return dub_error(L);
}

/** b2Vec2 b2Vec2::operator-() const
 * vendor/Box2D/Box2D/Common/b2Math.h:79
 */
static int b2Vec2_operator_unm(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    dub_pushudata(L, new b2Vec2(self->operator- ()), "b2.Vec2", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "operator- : %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "operator- : Unknown exception");
  }
  return dub_error(L);
}

/** float32 b2Vec2::operator()(int32 i) const
 * vendor/Box2D/Box2D/Common/b2Math.h:83
 */
static int b2Vec2_operator_call(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    int32 i = dub_checkint(L, 2);
    lua_pushnumber(L, self->operator()(i));
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "operator(): %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "operator(): Unknown exception");
  }
  return dub_error(L);
}

/** b2Vec2 b2Vec2::operator+(const b2Vec2 &b) const
 * vendor/Box2D/Box2D/Common/b2Math.h:95
 */
static int b2Vec2_operator_add(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    b2Vec2 *b = *((b2Vec2 **)dub_checksdata(L, 2, "b2.Vec2"));
    dub_pushudata(L, new b2Vec2(self->operator+(*b)), "b2.Vec2", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "operator+: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "operator+: Unknown exception");
  }
  return dub_error(L);
}

/** b2Vec2 b2Vec2::operator-(const b2Vec2 &b) const
 * vendor/Box2D/Box2D/Common/b2Math.h:101
 */
static int b2Vec2_operator_sub(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    b2Vec2 *b = *((b2Vec2 **)dub_checksdata(L, 2, "b2.Vec2"));
    dub_pushudata(L, new b2Vec2(self->operator-(*b)), "b2.Vec2", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "operator-: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "operator-: Unknown exception");
  }
  return dub_error(L);
}

/** b2Vec2 b2Vec2::operator*(float32 s) const
 * vendor/Box2D/Box2D/Common/b2Math.h:106
 */
static int b2Vec2_operator_mul(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    float32 s = dub_checknumber(L, 2);
    dub_pushudata(L, new b2Vec2(self->operator*(s)), "b2.Vec2", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "operator*: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "operator*: Unknown exception");
  }
  return dub_error(L);
}

/** void b2Vec2::operator+=(const b2Vec2 &v)
 * vendor/Box2D/Box2D/Common/b2Math.h:112
 */
static int b2Vec2_operator_adde(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    b2Vec2 *v = *((b2Vec2 **)dub_checksdata(L, 2, "b2.Vec2"));
    self->operator+=(*v);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "operator+=: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "operator+=: Unknown exception");
  }
  return dub_error(L);
}

/** void b2Vec2::operator-=(const b2Vec2 &v)
 * vendor/Box2D/Box2D/Common/b2Math.h:118
 */
static int b2Vec2_operator_sube(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    b2Vec2 *v = *((b2Vec2 **)dub_checksdata(L, 2, "b2.Vec2"));
    self->operator-=(*v);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "operator-=: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "operator-=: Unknown exception");
  }
  return dub_error(L);
}

/** void b2Vec2::operator*=(float32 a)
 * vendor/Box2D/Box2D/Common/b2Math.h:124
 */
static int b2Vec2_operator_mule(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    float32 a = dub_checknumber(L, 2);
    self->operator*=(a);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "operator*=: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "operator*=: Unknown exception");
  }
  return dub_error(L);
}

/** float32 b2Vec2::Length() const
 * vendor/Box2D/Box2D/Common/b2Math.h:130
 */
static int b2Vec2_Length(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    lua_pushnumber(L, self->Length());
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "Length: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "Length: Unknown exception");
  }
  return dub_error(L);
}

/** float32 b2Vec2::LengthSquared() const
 * vendor/Box2D/Box2D/Common/b2Math.h:137
 */
static int b2Vec2_LengthSquared(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    lua_pushnumber(L, self->LengthSquared());
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "LengthSquared: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "LengthSquared: Unknown exception");
  }
  return dub_error(L);
}

/** float32 b2Vec2::Normalize()
 * vendor/Box2D/Box2D/Common/b2Math.h:143
 */
static int b2Vec2_Normalize(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    lua_pushnumber(L, self->Normalize());
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "Normalize: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "Normalize: Unknown exception");
  }
  return dub_error(L);
}

/** bool b2Vec2::IsValid() const
 * vendor/Box2D/Box2D/Common/b2Math.h:158
 */
static int b2Vec2_IsValid(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    lua_pushboolean(L, self->IsValid());
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "IsValid: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "IsValid: Unknown exception");
  }
  return dub_error(L);
}

/** b2Vec2 b2Vec2::Skew() const
 * vendor/Box2D/Box2D/Common/b2Math.h:164
 */
static int b2Vec2_Skew(lua_State *L) {
  try {
    b2Vec2 *self = *((b2Vec2 **)dub_checksdata(L, 1, "b2.Vec2"));
    dub_pushudata(L, new b2Vec2(self->Skew()), "b2.Vec2", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "Skew: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "Skew: Unknown exception");
  }
  return dub_error(L);
}



// --=============================================== __tostring
static int b2Vec2___tostring(lua_State *L) {
  b2Vec2 *self = *((b2Vec2 **)dub_checksdata_n(L, 1, "b2.Vec2"));
  lua_pushfstring(L, "b2.Vec2: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2Vec2_member_methods[] = {
  { "__gc"         , b2Vec2__b2Vec2       },
  { "__newindex"   , b2Vec2__set_         },
  { "__index"      , b2Vec2__get_         },
  { "new"          , b2Vec2_b2Vec2        },
  { "SetZero"      , b2Vec2_SetZero       },
  { "Set"          , b2Vec2_Set           },
  { "__unm"        , b2Vec2_operator_unm  },
  { "__call"       , b2Vec2_operator_call },
  { "__add"        , b2Vec2_operator_add  },
  { "__sub"        , b2Vec2_operator_sub  },
  { "__mul"        , b2Vec2_operator_mul  },
  { "add"          , b2Vec2_operator_adde },
  { "sub"          , b2Vec2_operator_sube },
  { "mul"          , b2Vec2_operator_mule },
  { "Length"       , b2Vec2_Length        },
  { "LengthSquared", b2Vec2_LengthSquared },
  { "Normalize"    , b2Vec2_Normalize     },
  { "IsValid"      , b2Vec2_IsValid       },
  { "Skew"         , b2Vec2_Skew          },
  { "__tostring"   , b2Vec2___tostring    },
  { "deleted"      , dub_isDeleted        },
  { NULL, NULL},
};


extern "C" int luaopen_b2_Vec2(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.Vec2");
  // <mt>

  // register member methods
  luaL_register(L, NULL, b2Vec2_member_methods);
  // save meta-table in b2
  dub_register(L, "b2", "Vec2", "Vec2");
  // <mt>
  lua_pop(L, 1);
  return 0;
}
