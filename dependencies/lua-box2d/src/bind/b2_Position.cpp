/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2Position
 *
 * This file has been generated by dub 2.1.~.
 */
#include "dub/dub.h"
#include "Box2D/Dynamics/b2TimeStep.h"


/** Set attributes (key, value)
 * 
 */
static int b2Position__set_(lua_State *L) {

  b2Position *self = *((b2Position **)dub_checksdata_n(L, 1, "b2.Position"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub_hash(key, 3);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "c")) break;
      self->c = **((b2Vec2 **)dub_checksdata_n(L, 3, "b2.Vec2"));
      return 0;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "a")) break;
      self->a = luaL_checknumber(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2Position__get_(lua_State *L) {

  b2Position *self = *((b2Position **)dub_checksdata_n(L, 1, "b2.Position", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub_hash(key, 3);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "c")) break;
      dub_pushudata(L, &self->c, "b2.Vec2", false);
      return 1;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "a")) break;
      lua_pushnumber(L, self->a);
      return 1;
    }
  }
  return 0;
}

/** b2Position()
 * 
 */
static int b2Position_b2Position(lua_State *L) {
  try {
    b2Position *retval__ = new b2Position();
    dub_pushudata(L, retval__, "b2.Position", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "b2Position: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "b2Position: Unknown exception");
  }
  return dub_error(L);
}



// --=============================================== __tostring
static int b2Position___tostring(lua_State *L) {
  b2Position *self = *((b2Position **)dub_checksdata_n(L, 1, "b2.Position"));
  lua_pushfstring(L, "b2.Position: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2Position_member_methods[] = {
  { "__newindex"   , b2Position__set_     },
  { "__index"      , b2Position__get_     },
  { "new"          , b2Position_b2Position },
  { "__tostring"   , b2Position___tostring },
  { "deleted"      , dub_isDeleted        },
  { NULL, NULL},
};


extern "C" int luaopen_b2_Position(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.Position");
  // <mt>

  // register member methods
  luaL_register(L, NULL, b2Position_member_methods);
  // save meta-table in b2
  dub_register(L, "b2", "Position", "Position");
  // <mt>
  lua_pop(L, 1);
  return 0;
}
