/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2DistanceOutput
 *
 * This file has been generated by dub 2.1.~.
 */
#include "dub/dub.h"
#include "Box2D/Collision/b2Distance.h"


/** Set attributes (key, value)
 * 
 */
static int b2DistanceOutput__set_(lua_State *L) {

  b2DistanceOutput *self = *((b2DistanceOutput **)dub_checksdata_n(L, 1, "b2.DistanceOutput"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub_hash(key, 8);
  switch(key_h) {
    case 1: {
      if (DUB_ASSERT_KEY(key, "pointA")) break;
      self->pointA = **((b2Vec2 **)dub_checksdata_n(L, 3, "b2.Vec2"));
      return 0;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "pointB")) break;
      self->pointB = **((b2Vec2 **)dub_checksdata_n(L, 3, "b2.Vec2"));
      return 0;
    }
    case 5: {
      if (DUB_ASSERT_KEY(key, "distance")) break;
      self->distance = luaL_checknumber(L, 3);
      return 0;
    }
    case 6: {
      if (DUB_ASSERT_KEY(key, "iterations")) break;
      self->iterations = luaL_checkint(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2DistanceOutput__get_(lua_State *L) {

  b2DistanceOutput *self = *((b2DistanceOutput **)dub_checksdata_n(L, 1, "b2.DistanceOutput", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub_hash(key, 8);
  switch(key_h) {
    case 1: {
      if (DUB_ASSERT_KEY(key, "pointA")) break;
      dub_pushudata(L, &self->pointA, "b2.Vec2", false);
      return 1;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "pointB")) break;
      dub_pushudata(L, &self->pointB, "b2.Vec2", false);
      return 1;
    }
    case 5: {
      if (DUB_ASSERT_KEY(key, "distance")) break;
      lua_pushnumber(L, self->distance);
      return 1;
    }
    case 6: {
      if (DUB_ASSERT_KEY(key, "iterations")) break;
      lua_pushnumber(L, self->iterations);
      return 1;
    }
  }
  return 0;
}

/** b2DistanceOutput()
 * 
 */
static int b2DistanceOutput_b2DistanceOutput(lua_State *L) {
  try {
    b2DistanceOutput *retval__ = new b2DistanceOutput();
    dub_pushudata(L, retval__, "b2.DistanceOutput", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "b2DistanceOutput: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "b2DistanceOutput: Unknown exception");
  }
  return dub_error(L);
}



// --=============================================== __tostring
static int b2DistanceOutput___tostring(lua_State *L) {
  b2DistanceOutput *self = *((b2DistanceOutput **)dub_checksdata_n(L, 1, "b2.DistanceOutput"));
  lua_pushfstring(L, "b2.DistanceOutput: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2DistanceOutput_member_methods[] = {
  { "__newindex"   , b2DistanceOutput__set_ },
  { "__index"      , b2DistanceOutput__get_ },
  { "new"          , b2DistanceOutput_b2DistanceOutput },
  { "__tostring"   , b2DistanceOutput___tostring },
  { "deleted"      , dub_isDeleted        },
  { NULL, NULL},
};


extern "C" int luaopen_b2_DistanceOutput(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.DistanceOutput");
  // <mt>

  // register member methods
  luaL_register(L, NULL, b2DistanceOutput_member_methods);
  // save meta-table in b2
  dub_register(L, "b2", "DistanceOutput", "DistanceOutput");
  // <mt>
  lua_pop(L, 1);
  return 0;
}
